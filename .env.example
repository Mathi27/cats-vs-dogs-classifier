# PostgreSQL (used by docker-compose)
POSTGRES_USER=flaskuser
POSTGRES_PASSWORD=flaskpass
POSTGRES_DB=model_metrics

# Flask app - DATABASE_URL is built from above when using docker-compose
# For local dev without Docker: DATABASE_URL=postgresql://user:pass@localhost:5432/model_metrics
